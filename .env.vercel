# Database Configuration
DATABASE_URL="postgresql://your-production-db-url"

# Authentication (NextAuth.js)
NEXTAUTH_SECRET="generate-a-secure-32-char-or-longer-secret"
NEXTAUTH_URL="https://${VERCEL_URL}"

# Application Settings
NEXT_PUBLIC_APP_URL="https://${VERCEL_URL}"
VERCEL_URL="${VERCEL_URL}"

# Admin Configuration
INITIAL_ADMIN_PASSWORD="generate-a-secure-password-min-16-chars"

# Email Configuration (for password reset and notifications)
EMAIL_SERVER_HOST="your-smtp-server"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-smtp-username"
EMAIL_SERVER_PASSWORD="your-smtp-password"
EMAIL_FROM="noreply@yourdomain.com"

# API Configuration
API_URL="https://${VERCEL_URL}/api"

# Security and Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=900000

# Session Configuration
SESSION_SECRET="generate-another-secure-32-char-secret"

# Logging and Monitoring
LOG_LEVEL="error"

# Cache Configuration (if using Redis)
# REDIS_URL="your-redis-url"

# Comments explaining each variable:
# DATABASE_URL: Your PostgreSQL connection string (use connection pooling in production)
# NEXTAUTH_SECRET: Used to encrypt session tokens (generate with: openssl rand -base64 32)
# NEXTAUTH_URL: The canonical URL of your site (will be set automatically by Vercel)
# NEXT_PUBLIC_APP_URL: Publicly accessible URL of your application
# INITIAL_ADMIN_PASSWORD: First-time admin password (change after first login)
# EMAIL_*: SMTP server configuration for sending emails
# API_URL: Your API endpoint (usually same as your app URL + /api)
# RATE_LIMIT_*: API rate limiting configuration
# SESSION_SECRET: Additional encryption key for sessions
# LOG_LEVEL: Production logging configuration
# REDIS_URL: Optional - if you're using Redis for caching